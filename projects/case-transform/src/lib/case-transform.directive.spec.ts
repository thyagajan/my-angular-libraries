import { CaseTransformDirective } from './case-transform.directive';

describe('CaseTransformDirective', () => {
  it('should create an instance', () => {
    const directive = new CaseTransformDirective();
    expect(directive).toBeTruthy();
  });
});
